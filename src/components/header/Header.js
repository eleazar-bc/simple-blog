import React from 'react';
import './Header.css';

export default function Header() {
    return (
        <header>
            <h1>Simple Blog</h1>
            <button>Create Blog</button>
        </header>
    );
}
